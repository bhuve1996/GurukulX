import { ui } from "@/lib/config";

/** Default image for content cards when image_url is missing. Config-driven via ui.site.defaultCardImage. */
export const DEFAULT_CARD_IMAGE = ui.site.defaultCardImage;
